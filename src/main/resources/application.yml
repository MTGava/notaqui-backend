spring:
  jpa:
    hibernate:
      ddl-auto: create
    database: oracle
    show-sql: true

notaqui:
  backing-services:
    database:
      url: jdbc:oracle:thin:@oracle.fiap.com.br:1521:orcl
      driverClassName: oracle.jdbc.driver.OracleDriver
      username: ${USERNAME}
      password: ${PASSWORD}
      max-pool-size: 2
    google-cloud:
      url: https://vision.googleapis.com/v1/images:annotate
      api-key: AIzaSyDygvGEh508kmhoQI4xpwMX0UnIR9MKJ00

server:
  port: 8080
  servlet:
    context-path: /notaqui/api/v1